# oschess-cb-bridge agent guidance

Before acting in this repository, read `CLAUDE.md` completely and follow it as
the canonical project instructions. Those instructions apply to every model and
agent, including the required cross-model pull request review: every change is
reviewed by the backend — Codex or Claude — that did not write it, and never by
the one that did.
