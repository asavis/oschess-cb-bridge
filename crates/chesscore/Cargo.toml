[package]
name = "chesscore"
description = "A small chess core: legal moves, FEN, Chess960 and the Polyglot position key"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]

[dev-dependencies]
# Test oracle only: never a dependency of the shipped code.
cozy-chess = "0.3"
