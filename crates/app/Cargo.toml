[package]
name = "app"
description = "The oschess bridge for Windows: a tray app that serves this machine's ChessBase databases to oschess"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[[bin]]
name = "oschess-bridge-app"
path = "src/main.rs"

[dependencies]
bridge = { version = "0.1.0", path = "../bridge" }
# The updater's own decoding of its public key (base64, then minisign), so the
# key the app accepts is exactly the key the updater can use. Both are already
# in the tree through tauri-plugin-updater.
base64 = "0.22"
minisign-verify = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# The app itself runs on Windows only. Elsewhere the binary is a stub, so Linux
# builds need neither WebKitGTK nor a Windows resource compiler.
[target.'cfg(windows)'.dependencies]
tauri = { version = "=2.11.6", features = ["tray-icon", "image-png"] }
tauri-plugin-autostart = "=2.5.1"
tauri-plugin-clipboard-manager = "=2.3.3"
tauri-plugin-dialog = "=2.7.3"
tauri-plugin-notification = "=2.4.0"
tauri-plugin-opener = "=2.5.5"
tauri-plugin-single-instance = "=2.4.5"
# Updates from this repository's GitHub Releases, checked against our own key.
# TLS through Windows itself (schannel), which needs no C compiler for the
# cross-check in scripts/clippy-windows.sh, as rustls's ring would. The update
# is the NSIS installer itself, so the zip support is left out.
tauri-plugin-updater = { version = "=2.12.0", default-features = false, features = ["native-tls", "system-proxy"] }
# The display language and the taskbar's theme; already in the tree.
windows-sys = { version = "0.61", features = ["Win32_Foundation", "Win32_Globalization", "Win32_System_Registry"] }

[dev-dependencies]
cbformat = { version = "0.1.0", path = "../cbformat", features = ["fixture"] }

# Always a build dependency: a build script cannot tell a Windows target by
# `cfg`, so build.rs asks CARGO_CFG_TARGET_OS and runs it only for Windows.
[build-dependencies]
tauri-build = { version = "=2.6.3", features = [] }
